@ <stdio.hdr>

@ "gen.hdr"
@ "lexer.hdr"
@ "parser.hdr"
@ "genvar.hdr"

fn main()
{
    genInit();
    getToken();
    // parse program (sequence of statements)
    while (parseFunction()) {
    }
    if (token.kind != EOI) {
        printf("%u.%u: syntax error\n", token.pos.row, token.pos.col);
    }
    genHalt(3);
    genPrintSymtab();
}
